<%= form_for(@items_user) do |f| %>
  <% if @items_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@items_user.errors.count, "error") %> prohibited this items_user from being saved:</h2>

      <ul>
      <% @items_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
